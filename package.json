{
  "name": "moon-ui-monorepo",
  "version": "0.0.1",
  "description": "",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages",
    "docs"
  ],
  "scripts": {
    "dev": "npm run dev --workspace docs",
    "build": "npm run build --workspace packages",
    "changeset": "npm run changeset --workspace packages",
    "version": "npm run version --workspace packages",
    "release": "npm run release --workspace packages"
  }
}
