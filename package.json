{
  "name": "moon-ui-monorepo",
  "version": "0.0.1",
  "description": "",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages",
    "docs"
  ],
  "scripts": {
    "dev": "npm run dev --workspace docs",
    "build": "npm run build --workspace packages",
    "build-storybook": "npm run build-storybook --workspace docs",
    "changeset": "changeset",
    "version": "changeset version && npm install && npm update && npm run build && npm run test --workspace packages && node packages/dist/install.js --add-components",
    "release": "changeset publish"
  }
}
