{
  "name": "moon-ui-monorepo",
  "version": "0.0.1",
  "description": "",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages",
    "docs"
  ],
  "scripts": {
    "dev": "npm run sass:watch --workspace packages & npm run dev --workspace docs",
    "build": "npm run build --workspace packages",
    "build-storybook": "npm run build-storybook --workspace docs",
    "check-css-vars": "node check-css-variables.js",
    "changeset": "changeset",
    "version": "changeset version && npm install && npm update && npm run build && node packages/dist/install.js --add-components",
    "release": "changeset publish"
  }
}
