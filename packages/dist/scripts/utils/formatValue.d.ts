import type { FigmaColor, FigmaResolvedType } from "../../types.js";
declare const formatValue: (type: FigmaResolvedType, name: string, value: string | FigmaColor | number) => string | number | FigmaColor;
export default formatValue;
